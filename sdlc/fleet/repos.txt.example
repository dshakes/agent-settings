# compass fleet — the repos the cross-repo orchestrators operate on.
# Copy to `repos.txt` in your control repo. One `owner/name` per line; `#` comments.
# The orchestration workflows loop this list with `gh`. Scope your FLEET_TOKEN
# (fine-grained PAT or GitHub App) to EXACTLY these repos — least privilege.
your-org/service-a
your-org/service-b
# your-org/website
