# CLAUDE.md — working in the {{PROJECT}} repo

Auto-loaded by Claude Code (and AGENTS.md-aware tools) on entry. This is the
**single source of truth for repo conventions, invariants, and what NOT to do.**
Read it top-to-bottom before the first edit.

## What this is (one sentence)
{{ONE_SENTENCE}}

For the full picture: {{POINTERS — README, docs/architecture}}.

## Languages & commands
| Layer | Language | Notes |
|---|---|---|
| {{LAYER}} | {{LANG}} | {{WHY}} |

```bash
{{BUILD_CMD}}     # build
{{TEST_CMD}}      # test
{{LINT_CMD}}      # lint / format
{{RUN_CMD}}       # run locally
```

## Repo layout (only what an agent needs)
- `{{DIR}}` — {{PURPOSE}}

## Load-bearing invariants (the "what NOT to do")
> Violating these silently causes incidents. To change one, write an ADR first.
1. {{INVARIANT}}
2. {{INVARIANT}}

## Local development
{{HOW_TO_RUN_AND_VERIFY}}

## Conventions
- Errors: {{ERROR_STYLE}}
- Tests: {{TEST_STYLE}}
- {{OTHER}}

<!-- Generated by the bootstrap-agent-config skill. Replace every {{PLACEHOLDER}}
     with verified, repo-specific content. Delete sections that don't apply.
     Short and true beats long and aspirational. -->
